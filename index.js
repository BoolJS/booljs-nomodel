'use strict';

var async = require('async');

module.exports = {
    openDatabase: function () {
        return;
    },
    fetchModels: function (_instance, models) {
        console.log(models);
    }
};
